<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
    | <%= link_to_unless current_user?( user ), "delete", user, 
        method: :delete,
        data: { confirm: "You sure?" } %>
  <% end %>
</li>
