<%= form_for current_user.outfollowships.find_by( followee_id: @user.id ),
             html: { method: :delete },
             remote: true do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
